const SPRITE_CLIPPING_SIZE = new Vector2d(100, 100);
const MOVEMENT_SCALE_VALUE = 3;

const SPRITE_CLIPPING_DIRECTION = {
  right: 1 * SPRITE_CLIPPING_SIZE.y,
  left: 3 * SPRITE_CLIPPING_SIZE.y,
  up: 2 * SPRITE_CLIPPING_SIZE.y,
  down: 0,
};